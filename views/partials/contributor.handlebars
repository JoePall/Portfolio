<div class="card p-3 m-3" style="width: 18rem;">
    <img class="card-img-top" src="..." alt="profile of {{name}}">
    <div class="card-body">
        <h5 class="card-title">{{name}}</h5>
        <a href="{{url}}" class="btn btn-outline-primary">{{url}}</a>
    </div>
</div>